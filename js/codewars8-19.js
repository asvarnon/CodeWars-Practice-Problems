// (function () {
    "use strict";
    // ~~~~~~~~~~~~~~~~~~~a square of squares~~~~~~~~~~~~~~~~~

var isSquare = function(n){
    if (Number.isInteger(Math.sqrt(n))){
        return true;
    }
    return false;
}







// })();